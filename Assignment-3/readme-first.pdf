TESTING INSTRUCTIONS (Starting at the root file)
1. Move into the /app directory (cd app)
2. Install dependencies from the requirements.txt file (pip install -r requirements.txt)
3. Run the test script (python -m unittest test_app.py)

RUNNING SERVER INSTRUCTIONS (Starting at the root file)
1. Move into the /app directory (cd app)
2. Install dependencies from the requirements.txt file (if not done already)
3. Run the server (python app.py)

